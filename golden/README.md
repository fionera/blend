# Blender golden files

These files are used to generate all parsing code. They have to be named after this schema:

v{version}_{compressed|uncompressed}.blend

These files are a good example:
- v305_uncompressed.blend
- v305_compressed.blend
- v400_uncompressed.blend
- v400_compressed.blend